# Dependencies for running server.py (vision + robot WebSocket + Gemini Live).
# Install: pip install -r requirements.txt

numpy>=1.21
opencv-python>=4.5
PyAudio>=0.2.11
ultralytics>=8.0
google-genai>=1.0
websockets>=12.0

# Optional: face recognition (may need dlib; can fail on Apple Silicon)
# pip install face_recognition

# Optional: TSDF 3D mesh (/3d-mesh viewer). Requires Python 3.8–3.12 (no PyPI wheel for 3.13+).
# Use a Python 3.12 env then: pip install open3d
# open3d>=0.15
